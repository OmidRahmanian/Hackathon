# Posture Detection Client

Runs posture detection locally using webcam.

## Setup

1. Install Python 3.10+
2. Install dependencies:

pip install -r requirements.txt

3. Run:

python main.py

Make sure alert.wav is in the same folder.
